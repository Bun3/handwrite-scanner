# handwrite-scanner 사용 안내

프린트된 양식에 손으로 작성한 문서를 사진으로 올리면, 내용을 텍스트로 인식해
검색 가능한 PDF와 표(엑셀용 CSV 등)로 만들어 주는 프로그램입니다.
모든 처리가 PC/사내망 안에서 이루어지며 문서가 외부로 전송되지 않습니다.

> 흐름: **사진 업로드 → 자동 인식 → 검수·수정 → PDF·표 다운로드**

## 1. 설치와 실행

**내 PC에서만 쓸 때**
1. `handwrite-scanner.zip`을 원하는 폴더에 풉니다 (별도 설치 불필요).
2. `handwrite-scanner.exe` 실행 → 브라우저가 자동으로 열립니다.
3. **첫 실행에만** 인식 엔진(약 6GB)을 자동 다운로드합니다 (수십 분 소요 가능).

**사무실에서 여럿이 쓸 때 (서버 방식)**
1. PC 한 대(램 16GB 권장)에 설치하고 `server-mode.bat` 실행.
2. 콘솔에 표시되는 주소(예: `http://192.168.0.10:8000`)로 다른 PC들이 브라우저 접속.
3. 처음 켤 때 Windows 방화벽 허용 창이 뜨면 **허용**.

요구 사양: 램 16GB 권장(8GB는 경량 모델 설정), 디스크 여유 8GB+, GPU 불필요.
종료는 콘솔 창을 닫으면 됩니다.

## 2. 템플릿 만들기 — 인식률의 핵심 (양식마다 1회)

템플릿 없이도 동작하지만, 등록하면 정확도가 크게 올라갑니다.

1. 메뉴 **템플릿** → 이름 입력 → **빈 양식** 이미지/PDF 선택 → 기준 양식 등록
2. 양식 이미지 위에 **드래그로 손글씨 칸마다 박스**를 그림 (테두리에 걸쳐도 됨)
3. 각 박스의 라벨·타입 지정 후 **저장**

| 타입 | 어떤 칸에 | 예시 |
|---|---|---|
| 자유텍스트 | 아무 내용이나 적히는 칸 | 사유, 비고 |
| 후보목록 | 정해진 값 중 하나. 후보를 줄바꿈으로 입력 → 가장 비슷한 것 자동 선택 | 성명(직원 명단) |
| 전화번호 | 자릿수 검증·형식 정리 자동 | 연락처 |
| 숫자 | 범위(min~max) 지정 시 오독 자동 필터 | 월차잔여수 (0~11) |
| 표시 선택 | 선택지에 동그라미·체크·빗금 등이 표시되는 칸 | 승인종류 |

**요령**: 후보목록·숫자 범위를 최대한 채울수록 악필도 정확히 읽습니다.
특수한 칸은 "추가 지시"란에 설명을 적을 수 있습니다.

## 3. 문서 인식하기

- **작업** 화면에서 사진(여러 장 가능) 선택 + 템플릿 선택 → **인식 시작**
- 1장에 수 분 소요 — 여러 장 걸어두고 나중에 검수하는 방식 권장 (퇴근 전 걸어두면 다음날 완료)
- 같은 양식끼리 한 작업으로: 한 작업 = 같은 템플릿 = PDF 한 파일
- 휴대폰 사진도 OK (기울어짐 자동 보정)

## 4. 검수 — 색만 보면 됩니다

| 색 | 의미 | 할 일 |
|---|---|---|
| 초록 (100%) | 후보와 정확히 일치 | 넘어가도 됨 |
| 노랑 (76~99%) | 형식 검증 통과 | 훑어보기 |
| 주황 (51~75%) | 확신 낮음·자동 보정됨 | **원본과 대조** |
| 빨강 (50% 이하) | 인식 실패 가능성 | **반드시 확인** |

틀린 값은 입력란 클릭해 수정 — **즉시 저장**되고 초록으로 바뀝니다.

## 5. 결과 받기

- **미리보기** — 다운로드 없이 브라우저에서 PDF 확인
- **PDF (원본+텍스트층)** — 스캔본 모습 그대로 + 검색·복사 가능
- **표 내보내기** — CSV(엑셀용, 같은 양식이면 문서 1장=1행) / Markdown / 텍스트

## 6. 문제 해결

| 증상 | 해결 |
|---|---|
| PDF 열 때 "위험할 수 있음" 경고 | 웹 다운로드 파일에 붙는 Windows 표식(파일 문제 아님). 미리보기 사용, 또는 파일 우클릭→속성→차단 해제 |
| 다른 PC에서 접속 안 됨 | 서버 PC 방화벽 허용 필요 — server-mode.bat 재실행 후 허용 |
| "정합 실패" 경고 | 사진이 심하게 비뚤거나 다른 양식. 정면으로 다시 촬영 |
| 너무 느림/멈춤 | 1장에 수 분이 정상. 램 부족 시 경량(3B) 모델 설정 |
| 백업 | 프로그램 폴더의 `data\` 폴더만 복사하면 전체 백업 |
